#!/usr/bin/env bash
set -euo pipefail

# Compatibility evidence for dashboard_control_center_agentic_control_tower_p0_p10.
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/dashboard-review-manifest" "$@"
